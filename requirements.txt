# Production dependencies. Install with:
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements.txt
#
# See requirements-dev.txt for the extra tools (pytest, ruff) needed to
# work on the project itself, not just run it. This file is the single
# source of truth for what the app needs — pyproject.toml only holds
# project metadata and tool config (ruff/pytest settings) now, not a
# second copy of this list.

fastapi>=0.115.6
uvicorn[standard]>=0.34.0
sqlalchemy>=2.0.36
aiosqlite>=0.20.0
httpx>=0.28.1
python-dotenv>=1.0.1
jinja2>=3.1.5
python-multipart>=0.0.20
numpy>=2.2.1
pypdf>=5.1.0
psutil>=6.1.0
python-docx>=1.1.2
striprtf>=0.0.28
odfpy>=1.4.1
bcrypt>=4.2.0
itsdangerous>=2.2.0
alembic>=1.19.2

# Renames this process from the generic "python" (what every tool that
# reads /proc/PID/comm — ps, top, ss -p, etc. — shows by default for
# any Python script) to "pAIring-server" instead. See run.py.
setproctitle>=1.3.3

# Every database backend this app supports (see "Database & migrations"
# in README.md and Settings > System > Database) is installed by
# default, not behind a separate step — nothing extra to remember to
# pick SQLite or MySQL from that settings page. Both MySQL drivers, not
# just one: Alembic (alembic/env.py) and app/database.py's own startup
# schema check build a *sync* engine straight from DATABASE_URL as
# given, so pymysql has to be there for those — while the app's own
# request-serving engine is async (see app/database.py's _async_url),
# which needs aiomysql instead. cryptography backs MySQL 8's default
# caching_sha2_password auth plugin, used by both drivers.
pymysql>=1.2.0
aiomysql>=0.2.0
cryptography>=50.0.1

# Instrumentation for the Telemetry data API + SDK only, no OTLP/collector
# exporter package: spans are exported straight into our own DB via a
# hand-written in-process exporter, not to an external OTel backend.
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
